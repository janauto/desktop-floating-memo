# 🎯 桌面浮窗备忘录 — 操作说明

> **版本**: v1.0.0  
> **平台**: macOS（Electron 应用）  
> **定位**: 游戏化桌面任务管理工具

---

## 📋 目录

1. [安装与启动](#1-安装与启动)
2. [主窗口概览](#2-主窗口概览)
3. [任务管理](#3-任务管理)
4. [番茄时钟](#4-番茄时钟)
5. [游戏化系统](#5-游戏化系统)
6. [控制中心（设置面板）](#6-控制中心设置面板)
7. [系统托盘](#7-系统托盘)
8. [快捷操作汇总](#8-快捷操作汇总)
9. [数据与存储](#9-数据与存储)
10. [常见问题](#10-常见问题)

---

## 1. 安装与启动

### 环境要求

| 依赖 | 最低版本 |
|------|---------|
| Node.js | v16+ |
| npm | v8+ |
| macOS | 10.15+ |

### 安装步骤

```bash
# 1. 进入项目根目录
cd 桌面浮窗备忘录

# 2. 安装依赖
npm install

# 3. 重建原生模块（better-sqlite3）
npm run rebuild

# 4. 启动应用
npm start
```

### 打包发布

```bash
npm run build
```

打包后将生成 `.dmg` 安装文件，可分发给其他 macOS 用户。

---

## 2. 主窗口概览

应用启动后，桌面右上角会出现一个半透明的浮窗。整体界面从上到下依次为：

```
┌──────────────────────────────┐
│ 🎯 浮窗备忘录  00:00:00  Lv.1 ⚙ ─ │  ← 标题栏
│ ████████░░░░░  30 / 100 XP      │  ← 经验条
│ 已打掉: 5  │ 今日: 2  │ 连击: 0  │  ← 统计行
│ ─────────────────────────────── │
│ 🎯 专注  │ ☕ 短休  │ 🌙 长休    │  ← 番茄时钟
│          25:00                  │
│        ▶ ⟲ ⏭                   │
│ ─────────────────────────────── │
│ "每一个不曾起舞的日子..."        │  ← 励志语录
│ ─────────────────────────────── │
│ [ 输入任务，回车添加... ] 🟢🟡🔴 │  ← 输入区
│ 全部 | 需求 | 会议 | 💡想法 | 🐛Bug │ ← 标签筛选
│ ─────────────────────────────── │
│  ⚡ 任务名称           ⏱ 00:05  │  ← 任务卡片
│  ⚡ 另一个任务         ⏱ 01:23  │
└──────────────────────────────┘
```

### 窗口行为

| 操作 | 效果 |
|------|------|
| **拖拽标题栏** | 移动窗口位置（位置会自动保存） |
| **双击标题栏** | 折叠 / 展开窗口 |
| **点击 `─` 按钮** | 折叠窗口（仅保留标题栏） |
| **窗口失去焦点** | 自动变为半透明（默认 55% 不透明度） |
| **鼠标移入窗口** | 恢复完全不透明 |

---

## 3. 任务管理

### 3.1 添加任务

1. 在输入框中输入任务内容
2. **选择优先级**（可选，默认为普通）：
   - 🟢 **普通**：完成获得 10 XP
   - 🟡 **重要**：完成获得 20 XP
   - 🔴 **紧急**：完成获得 30 XP
3. **选择标签**（可选）：点击标签栏上的标签可以给新任务自动打上标签
4. 按 **Enter** 键添加任务

> 💡 如果当前选中了某个标签筛选（如「需求」），新添加的任务会自动关联该标签。

### 3.2 完成任务（打掉！）

点击任务卡片左侧的 **⚡ 按钮** 即可「打掉」该任务：

- 🎆 触发粒子爆炸特效
- 📳 屏幕震动反馈（可在设置中关闭）
- ⭐ 获得对应优先级的经验值
- 🔥 如在连击时间窗口内连续完成，触发 Combo 加成

### 3.3 删除任务

点击任务卡片右侧的 **✕ 按钮** 可以直接删除任务（不获得经验值）。

### 3.4 任务计时

每个任务卡片都有一个内置计时器，记录该任务从创建到完成的耗时：

| 操作 | 说明 |
|------|------|
| **⏸ / ▶ 按钮** | 暂停 / 继续计时 |
| **计时颜色** | 🟢 \< 4小时 → 🟡 \< 24小时 → 🔴 \> 24小时 |

### 3.5 标签筛选

点击标签栏中的标签按钮可以按标签筛选任务列表：

- **全部**：显示所有任务
- **需求 / 会议 / 💡想法 / 🐛Bug**：仅显示对应标签的任务

### 3.6 拖拽排序

直接拖拽任务卡片可以调整任务顺序，松开后自动保存。

---

## 4. 番茄时钟

### 4.1 基本操作

番茄时钟区域位于统计行下方，支持三种模式：

| 模式 | 默认时长 | 说明 |
|------|---------|------|
| 🎯 **专注** | 25 分钟 | 工作专注时间 |
| ☕ **短休** | 5 分钟 | 短暂休息 |
| 🌙 **长休** | 15 分钟 | 长时间休息（每4轮后） |

### 4.2 控制按钮

| 按钮 | 功能 |
|------|------|
| **▶ / ⏸** | 开始 / 暂停计时 |
| **⟲** | 重置当前计时 |
| **⏭** | 跳过当前阶段 |
| **▾** | 收起 / 展开番茄时钟区域 |

### 4.3 工作流程

```
专注(25分钟) → 短休(5分钟) → 专注 → 短休 → 专注 → 短休 → 专注 → 长休(15分钟)
   第1轮          第2轮          第3轮          第4轮         重新开始
```

- 每完成一轮 **专注** 模式将获得 **15 XP** 奖励
- 完成时会播放提示音通知
- 底部进度点显示当前轮次（● ● ● ○ = 已完成3轮，第4轮进行中）

---

## 5. 游戏化系统

### 5.1 经验值（XP）和等级

- 完成任务获得 XP，XP 满 **100** 自动升级
- 等级badge显示在标题栏右侧（如 `Lv.5`）
- 升级时触发 **LEVEL UP!** 全屏特效

### 5.2 连击系统（Combo）

在设定的时间窗口（默认 5 秒）内连续完成多个任务可触发连击：

| 连击数 | 额外 XP |
|--------|---------|
| 2x | +10 |
| 3x | +15 |
| 5x | +25 |
| 10x | +50 |

连击数显示在经验条下方，超过时间窗口未完成新任务则连击重置。

### 5.3 成就系统

完成特定条件后解锁成就（每个成就额外奖励 **50 XP**）：

| 成就 | 名称 | 解锁条件 |
|------|------|----------|
| 🏆 | 初次出击 | 完成第 1 个任务 |
| 🏆 | 小试牛刀 | 完成第 10 个任务 |
| 🏆 | 效率达人 | 完成第 50 个任务 |
| 🏆 | 任务终结者 | 完成第 100 个任务 |
| 🏆 | 三连击 | 达成 3 连击 |
| 🏆 | 五杀！ | 达成 5 连击 |
| 🏆 | 无人能挡 | 达成 10 连击 |
| 🏆 | 崭露头角 | 达到 Lv.5 |
| 🏆 | 久经沙场 | 达到 Lv.10 |

成就解锁时会弹出金色提示横幅。

---

## 6. 控制中心（设置面板）

点击标题栏的 **⚙ 齿轮按钮** 打开控制中心窗口。

### 6.1 📊 看板分析

数据看板页面展示：

- **今日完成数** / **本周完成数** / **完成率** / **累计经验**
- **活跃时间段**：24小时柱状图，显示你最高效的工作时段
- **最近7日趋势**：每日完成任务数走势图
- **AI 效率诊断**：根据你的数据给出针对性建议
- **励志语录**：每 5 分钟自动刷新

### 6.2 ⚙️ 全局设置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 普通任务经验值 | 10 | 完成普通任务获得的 XP |
| 重要任务经验值 | 20 | 完成重要任务获得的 XP |
| 紧急任务经验值 | 30 | 完成紧急任务获得的 XP |
| 连击判定时间 | 5000ms | 连续完成任务时触发连击的最大间隔 |
| 始终置顶 | ✅ 开启 | 主窗口是否始终在最前面 |
| 屏幕震动 | ✅ 开启 | 打掉任务时是否产生震动效果 |

### 6.3 🎨 个性化

- **失焦透明度**：调整窗口失去焦点时的透明程度（0.10 ~ 1.00）
- **标签管理**：
  - 点击标签上的 **✕** 删除已有标签
  - 在输入框中输入并按 **Enter** 添加新标签

### 6.4 💻 终端

内置命令行终端，支持通过文本命令精确调整系统参数。

#### 可用命令

| 命令 | 用法 | 说明 |
|------|------|------|
| `help` | `help` | 显示所有可用命令 |
| `list` | `list` | 列出所有当前配置参数及其值 |
| `get` | `get <参数名>` | 查看指定参数的值 |
| `set` | `set <参数名> <值>` | 修改指定参数的值 |
| `clear` | `clear` | 清空终端屏幕 |
| `exit` | `exit` | 关闭控制中心 |

#### 终端使用示例

```bash
# 查看所有配置
admin@memo:~$ list

# 查看紧急任务经验值
admin@memo:~$ get xpUrgent

# 将紧急任务经验值设为 50
admin@memo:~$ set xpUrgent 50

# 关闭屏幕震动
admin@memo:~$ set screenShake false

# 修改连击判定时间为 8 秒
admin@memo:~$ set comboWindow 8000
```

#### 可配置的参数列表

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `xpNormal` | 数值 | 普通任务经验值 |
| `xpImportant` | 数值 | 重要任务经验值 |
| `xpUrgent` | 数值 | 紧急任务经验值 |
| `comboWindow` | 数值 | 连击判定窗口（毫秒） |
| `blurOpacity` | 小数 | 失焦透明度 |
| `alwaysOnTop` | 布尔 | 是否始终置顶 |
| `showClock` | 布尔 | 是否显示时钟 |
| `showTaskTimer` | 布尔 | 是否显示任务计时 |
| `screenShake` | 布尔 | 是否开启震动效果 |
| `tags` | 数组 | 标签列表 |

---

## 7. 系统托盘

应用运行时会在 macOS 菜单栏显示一个托盘图标。

| 操作 | 说明 |
|------|------|
| **点击托盘图标** | 显示 / 聚焦主窗口 |
| **右键托盘图标** | 打开菜单 |

### 托盘右键菜单

| 菜单项 | 功能 |
|--------|------|
| 显示/隐藏 | 切换主窗口的显示状态 |
| 始终置顶 | 切换窗口置顶状态（带勾选标记） |
| 退出 | 完全退出应用 |

---

## 8. 快捷操作汇总

| 操作 | 快捷方式 |
|------|----------|
| 添加任务 | 输入框输入内容 + `Enter` |
| 完成（打掉）任务 | 点击 ⚡ 按钮 |
| 删除任务 | 点击 ✕ 按钮 |
| 暂停/继续任务计时 | 点击 ⏸/▶ 按钮 |
| 折叠/展开窗口 | 双击标题栏 或 点击 `─` 按钮 |
| 打开控制中心 | 点击 ⚙ 按钮 |
| 刷新励志语录 | 点击语录区域 |
| 拖拽排序 | 拖拽任务卡片 |

---

## 9. 数据与存储

### 存储位置

| 数据类型 | 存储方式 | 位置 |
|----------|---------|------|
| 任务列表 & 游戏状态 | electron-store (JSON) | `~/Library/Application Support/desktop-floating-memo/config.json` |
| 任务历史记录 & 统计 | SQLite 数据库 | `~/Library/Application Support/desktop-floating-memo/memo_history.db` |

### 数据安全

- 任务数据在每次操作后**自动保存**
- 窗口位置在每次拖拽后**自动记忆**
- SQLite 使用 **WAL 模式**，保证写入可靠性
- 关闭应用时数据库会**安全关闭**

---

## 10. 常见问题

### Q: 应用启动后报 `better-sqlite3` 相关错误？

**A:** 需要针对当前 Electron 版本重新编译原生模块：

```bash
npm run rebuild
```

如果仍然报错，尝试清除缓存后重新安装：

```bash
rm -rf node_modules
npm install
npm run rebuild
```

### Q: 窗口太透明看不清？

**A:** 打开控制中心 → 个性化 → 将「失焦透明度」调高（推荐 0.75 以上），或者在终端中执行：

```bash
set blurOpacity 0.85
```

### Q: 如何重置所有游戏进度？

**A:** 删除配置文件即可重置：

```bash
rm ~/Library/Application\ Support/desktop-floating-memo/config.json
```

重启应用后所有等级、XP、成就将回到初始状态。

### Q: 任务历史数据如何清除？

**A:** 删除数据库文件：

```bash
rm ~/Library/Application\ Support/desktop-floating-memo/memo_history.db
```

### Q: 窗口消失了怎么找回？

**A:** 点击 macOS 菜单栏中的托盘图标，选择「显示/隐藏」即可。

---

## 📁 项目结构

```
桌面浮窗备忘录/
├── main.js                 # Electron 主进程（窗口管理、IPC 通信）
├── db.js                   # SQLite 数据库模块（任务历史、统计）
├── package.json            # 项目配置
├── assets/
│   └── tray-icon.png       # 系统托盘图标
└── renderer/
    ├── index.html          # 主窗口页面
    ├── styles.css          # 主窗口样式
    ├── app.js              # 核心应用逻辑（任务管理、游戏化）
    ├── pomodoro.js          # 番茄时钟模块
    ├── quotes.js            # 励志语录库
    ├── preload.js           # Electron 预加载脚本（安全桥接）
    ├── settings.html        # 控制中心页面
    ├── settings.css         # 控制中心样式
    └── settings.js          # 控制中心逻辑（看板、设置、终端）
```

---

> 🎮 **享受「打掉任务」的快感，让每一次完成都有仪式感！**
